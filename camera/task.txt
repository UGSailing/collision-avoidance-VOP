Now I want to make a file that activates the venv of the hailo (source /home/mario/Documents/hailo-apps/setup_env.sh) and then runs the first_working_camerasystem. Also, I want you to change the output of first_working_camerasystem.py  so it is able to output detected objects to a usb (should be able to be chosen in config.py, for example /dev/ttyUSB1) and in format <angle_deg>,<distance_m>; 